/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['foshati.storage.c2.liara.space', 'avatars.githubusercontent.com'],
  },
  output: 'standalone',
};

export default nextConfig;
